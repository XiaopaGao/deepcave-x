# 测试者操作说明

1. 在自己的数据集上完成 Optuna 或 SMAC 超参数优化。
2. Optuna 上传 CSV 或本站格式 JSON；SMAC 2.x 上传包含三个原生 JSON 的 ZIP。
3. “数据集／任务标识”填写能区分数据划分的名称，例如 `breast_cancer_seed42_cv5`。需要比较的两次运行填写相同标识。
4. Optuna 准确率 CSV 选择“越高越好”。SMAC 如果目标函数返回 `1 - accuracy`，选择“错误率转准确率”；如果返回普通 loss，则保留原始指标。
5. 运行 A 用于重要性和瓶颈诊断；A、B 共同用于轨迹对比。
6. 比较前确认数据集、划分、指标、搜索空间和预算一致。
7. 不要上传原始数据集、训练模型、密码、令牌或个人信息。

测试时记录：是否成功导入、轨迹视图是否清楚、最重要的两个参数、检测到的停滞区间、修改 patience 后的变化、完成时间和最困惑的地方。反馈在线下交给作业作者，网站不收集反馈。

